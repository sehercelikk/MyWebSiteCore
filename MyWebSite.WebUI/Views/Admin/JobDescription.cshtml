@model List<JobDescription>
@{
    ViewData["Title"] = "JobDescription";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}


<div class="card shadow p-5" style="width: 100%; border: none; border-radius: 0;">
    <h4 class="text-center mb-4 text-primary">Görevler</h4>
    <table class="table table-bordered">
        <tr class="text-center">
            <th>SIRA</th>
            <th>GÖREV TANIMI</th>
            <th>İŞLEMLER</th>
        </tr>
        <tbody>
            @foreach (var item in Model)
            {

                <tr>
                    <th>@item.Id</th>
                    <td>@item.JobTitle</td>
                    <td>
                        <div style="display: flex; gap: 8px;">
                            <a href="/Admin/DeleteJobDescription/@item.Id" class="btn btn-danger btn-sm">Sil</a>
                            <a href="/Admin/UpdateJobDescription/@item.Id" class="btn btn-primary btn-sm">Güncelle</a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
<br />
<a href="/Admin/AddJobDescription" class="btn btn-success btn-sm">Ekle</a>


